import { MessageCountDirective } from './message-count.directive';

describe('MessageCountDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageCountDirective();
    expect(directive).toBeTruthy();
  });
});
