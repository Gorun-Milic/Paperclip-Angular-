import { SaveDirective } from './save.directive';

describe('SaveDirective', () => {
  it('should create an instance', () => {
    const directive = new SaveDirective();
    expect(directive).toBeTruthy();
  });
});
